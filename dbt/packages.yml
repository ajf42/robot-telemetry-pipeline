# Run `dbt deps` before `dbt build`. dbt_utils supplies accepted_range, used to
# bound temp_c in models/staging/schema.yml.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
